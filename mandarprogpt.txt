arquivo components.css

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
}

.grid-cards {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
}

.metric-card {
  padding: 20px;
}

.metric-card span {
  display: block;
  font-size: 14px;
  color: var(--text-soft);
  margin-bottom: 10px;
}

.metric-card strong {
  font-size: 28px;
}

.toolbar {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 18px;
  flex-wrap: wrap;
}

.toolbar-left,
.toolbar-right {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.input,
.select {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 12px 14px;
  min-width: 220px;
  outline: none;
}

.input:focus,
.select:focus {
  border-color: var(--primary);
}

.btn,
.btn-outline,
.btn-danger {
  border: none;
  border-radius: 12px;
  padding: 12px 16px;
  cursor: pointer;
  transition: 0.2s;
}

.btn {
  background: var(--primary);
  color: white;
}

.btn:hover {
  background: var(--primary-hover);
}

.btn-outline {
  background: white;
  border: 1px solid var(--border);
}

.btn-danger {
  background: var(--danger);
  color: white;
}

.table-card {
  overflow: hidden;
}

.table-wrapper {
  overflow-x: auto;
}

.table {
  width: 100%;
  border-collapse: collapse;
  background: white;
}

.table th,
.table td {
  text-align: left;
  padding: 14px 16px;
  border-bottom: 1px solid var(--border);
}

.table th {
  background: var(--surface-2);
  font-size: 14px;
  color: var(--text-soft);
}

.badge {
  display: inline-block;
  padding: 6px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
}

.badge-success {
  background: #dcfce7;
  color: #166534;
}

.badge-warning {
  background: #fef3c7;
  color: #92400e;
}

.badge-danger {
  background: #fee2e2;
  color: #991b1b;
}

.actions {
  display: flex;
  gap: 8px;
}

.empty-state {
  padding: 24px;
  text-align: center;
  color: var(--text-soft);
}

@media (max-width: 1100px) {
  .grid-cards {
    grid-template-columns: repeat(2, 1fr);
  }

  .sidebar {
    width: 220px;
  }
}

@media (max-width: 768px) {
  .app-shell {
    flex-direction: column;
  }

  .sidebar {
    width: 100%;
  }

  .grid-cards {
    grid-template-columns: 1fr;
  }
}

arquivo global.css

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --bg: #f5f7fb;
  --surface: #ffffff;
  --surface-2: #f8fafc;
  --primary: #2563eb;
  --primary-hover: #1d4ed8;
  --text: #111827;
  --text-soft: #6b7280;
  --border: #e5e7eb;
  --danger: #dc2626;
  --success: #16a34a;
  --warning: #d97706;
  --shadow: 0 10px 30px rgba(15, 23, 42, 0.08);
  --radius: 16px;
}

body {
  font-family: Arial, Helvetica, sans-serif;
  background: var(--bg);
  color: var(--text);
}

button,
input,
select,
textarea {
  font: inherit;
}

arquivo layout.css

.app-shell {
  display: flex;
  min-height: 100vh;
}

.sidebar {
  position: fixed;
  top: 0;
  left: 0;

  width: 250px;
  height: 100vh;

  background: #0f172a;
  color: white;

  padding: 24px 15px;

  display: flex;
  flex-direction: column;
  justify-content: space-between;

  overflow-y: auto;
}

.sidebar-brand {
  font-size: 22px;
  font-weight: 700;
  letter-spacing: 0.5px;
}

.sidebar-subtitle {
  font-size: 13px;
  color: #94a3b8;
}

.sidebar nav {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.sidebar-link {
  color: #e5e7eb;
  text-decoration: none;
  padding: 12px 14px;
  border-radius: 12px;
  transition: 0.2s;
}

.sidebar-link:hover,
.sidebar-link.active {
  background: rgba(255, 255, 255, 0.12);
  transform: translateX(4px);
}

.main-content {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.topbar {
  height: 72px;
  background: var(--surface);
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 24px;
}

.page-container {
  padding: 24px;
}

.page-header {
  margin-bottom: 20px;
}

.page-header h1 {
  font-size: 28px;
  margin-bottom: 6px;
}

.page-header p {
  color: var(--text-soft);
}

.main-content {
  margin-left: 250px; /* 🔥 empurra pro lado */
  flex: 1;
  display: flex;
  flex-direction: column;
}

.app-shell {
  min-height: 100vh;
}

arquivo topbar.html

<div class="topbar">
  <div>
    <strong id="topbar-title">Dashboard</strong>
  </div>

  <div>
    <span style="color:#6b7280;">Sistema local empresarial</span>
  </div>
</div>

arquivo sidebar.html


<div class="sidebar">
  <div>
    <div class="sidebar-brand">Core ERP</div>
    <div class="sidebar-subtitle">Gestão inteligente</div>
  </div>

  <nav>
    <a class="sidebar-link" href="#dashboard">Dashboard</a>
    <a class="sidebar-link" href="vendas">Vendas</a>
    <a class="sidebar-link" href="#produtos">Produtos</a>
    <a class="sidebar-link" href="#movimentacoes">Movimentações</a>
    <a class="sidebar-link" href="#fornecedores">Fornecedores⏳</a>
  </nav>
</div>